Arch:
  name: largeconvnet
  input_dim: 3
  num_classes: 10
  stochastic: false

Optim:
  name: Adam
  lr: 0.001

DataLoader:
  batch_size: 100
  num_workers: 8
  shuffle: True
  drop_last: True

Trainer:
  name: vat
  device: cuda
  save_dir: first_try
  max_epoch: 100

